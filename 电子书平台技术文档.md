# 「云阅 CloudRead」电子书平台 — 技术设计文档

## 一、 文档概述

### 1.1 编写目的

本文档旨在为「云阅 CloudRead」电子书共享与管理平台提供全面、严谨的技术设计与规范说明。本文档将作为前后端开发、数据库设计、系统测试及后期运维的核心指导依据，确保项目团队对系统架构、业务逻辑和技术实现细节达成共识。

### 1.2 系统定位

本项目是一个轻量级、可溯源、高互动的B/S架构电子书平台。系统分为客户端（面向普通用户）和管理端（面向系统管理员），核心技术栈为 Spring Boot + Vue + SQLite。

---

## 二、 系统总体架构设计

### 2.1 架构模式

系统采用前后端分离的 B/S（浏览器/服务器）架构。前端通过 HTTP/HTTPS 协议与后端进行 RESTful API 通信，部分实时日志推送采用 WebSocket 协议。

### 2.2 逻辑分层架构

系统后端自上而下划分为四层：

1. **接入层**：负责请求路由、跨域处理、JWT Token 校验、限流与防刷。
2. **业务逻辑层**：实现用户认证、书籍管理、互动社区、收藏点赞等核心业务逻辑。
3. **领域服务层**：封装独立的技术领域服务，包括文件存储服务、溯源引擎服务、全文检索服务、系统监控服务。
4. **数据访问层**：通过 ORM 框架与 SQLite 数据库交互，管理本地文件系统及日志文件。

### 2.3 技术选型及依据


| 技术领域        | 选型方案                  | 选型依据                                        |
| :----------- | :--------------------- | :------------------------------------------- |
| **前端框架**    | Vue 3 + Vite          | 组合式 API 提升代码复用率，Vite 提供极速冷启动和热更新。           |
| **前端 UI 库** | Element Plus          | 组件丰富、文档完善，适合快速构建企业级中后台及客户端界面。               |
| **后端框架**    | Spring Boot 3.x       | 生态成熟，自动配置机制大幅降低搭建成本，内置 Tomcat 易于部署。         |
| **ORM 框架**  | MyBatis-Plus          | 简化单表 CRUD 操作，提供丰富的条件构造器，良好支持 SQLite 方言。     |
| **安全认证**    | Spring Security + JWT | 采用无状态的 JWT 机制，适合前后端分离架构，避免服务端 Session 存储压力。 |
| **数据库**     | SQLite 3 (WAL模式)      | 零配置、单文件部署，极大降低初期运维成本；开启 WAL 模式以优化并发读写性能。    |
| **文件存储**    | 本地文件系统                | 初期采用服务器本地磁盘存储，按日期和文件哈希分目录，预留对象存储（OSS）迁移接口。  |
| **接口文档**    | Knife4j (Swagger 3)   | 自动扫描注解生成在线 API 文档，支持接口在线调试。                 |


---

## 三、 功能模块详细设计

### 3.1 客户端功能模块

#### 3.1.1 用户认证模块

- **登录与自动注册**：用户仅需输入用户名和密码。系统以用户名作为全局唯一标识符。若用户名不存在，系统自动将其注册为新用户，密码经 BCrypt 算法加密后落库。
- **会话管理**：登录成功后签发 JWT Token，客户端将其存储于 LocalStorage 并在后续请求的 Header 中携带。Token 设置合理过期时间，并支持无感续签。

#### 3.1.2 电子书管理模块

- **电子书上传（含封面处理）**：
  - 支持 PDF、EPUB、TXT、MOBI 格式文件上传。
  - **封面上传逻辑（新增）**：用户在上传时**可选**手动上传封面图片（支持 JPG/PNG 格式）。若用户未提供封面，系统将尝试解析电子书文件内部的元数据提取封面；若解析失败，系统将根据书名和作者自动生成一张默认的文字排版封面。
  - 上传时系统自动计算文件的 SHA-256 哈希值作为唯一数字指纹。
- **分类与检索**：支持多级分类树形展示。提供全局搜索和分类内搜索，支持按书名、作者、ISBN 进行模糊匹配。
- **在线阅读与下载**：提供基于 Web 的阅读器（PDF 渲染、TXT 翻页），并支持原文件下载，系统自动记录阅读进度和下载次数。

#### 3.1.3 互动社区与收藏模块

- **评论与回复**：每本书拥有独立评论区，支持发表文字评论及最多两层的嵌套回复。
- **点赞机制**：支持对评论进行点赞，系统通过联合唯一索引确保同一用户对同一评论仅能点赞一次。
- **收藏管理**：书籍详情页提供收藏/取消收藏开关，用户可在“个人中心-我的收藏”中按时间倒序查看和管理收藏列表。

#### 3.1.4 书籍溯源模块

- **溯源信息展示**：在书籍详情页展示溯源面板，包含上传者信息（脱敏）、上传时间、文件哈希值、历史版本变更记录。
- **防篡改校验**：用户或管理员点击“校验”时，系统实时重新计算物理文件的哈希值并与数据库记录比对，直观展示文件是否被篡改。

### 3.2 管理端功能模块

#### 3.2.1 全栈可观测性中心

- **后端运行监控**：集成 Spring Boot Actuator，实时展示 JVM 内存分配、CPU 负载、线程池状态、GC 频率及 HTTP 接口响应指标。
- **前端运行监控**：接收前端埋点上报数据，统计页面加载耗时（FCP/LCP）、JS 异常捕获率、API 请求成功率。
- **实时日志流**：通过 WebSocket 将后端 ERROR 和 WARN 级别日志实时推送到管理端面板，支持按关键字和日志级别过滤。
- **数据库状态监控**：展示 SQLite 数据库文件大小、WAL 文件体积、连接池活跃/空闲连接数及慢查询记录。

#### 3.2.2 内容与用户管理

- **内容审核**：对新上传的书籍进行人工或自动审核，管理分类树，处理违规评论。
- **溯源审计**：查看全局书籍的溯源链条，处理文件篡改告警事件。
- **用户管理**：查看用户列表、行为日志，执行封禁/解封操作。
- **系统配置**：动态调整文件大小限制、允许的格式白名单、注册与审核开关。

---

## 四、 数据模型设计

### 4.1 实体关系（ER）说明

- **用户与书籍**：一对多关系（一个用户可上传多本书）。
- **书籍与分类**：多对一关系（一本书归属一个末级分类）。
- **用户与评论/点赞/收藏**：均为一对多关系。
- **书籍与溯源日志/版本记录**：一对多关系（一本书拥有多条操作日志和多个历史版本）。

### 4.2 核心数据表结构设计（无代码描述）

#### 4.2.1 用户表 (sys\_user)

- **id**：主键，自增整数。
- **username**：字符串，唯一索引，作为用户唯一标识符。
- **password**：字符串，存储 BCrypt 加密后的密文。
- **role**：字符串，标识用户角色（普通用户/管理员）。
- **status**：整数，标识账号状态（正常/封禁）。
- **created\_at / updated\_at**：时间戳，记录创建与更新时间。

#### 4.2.2 电子书表 (book)

- **id**：主键，自增整数。
- **title / author / description**：字符串/文本，存储书籍基础元数据。
- **cover\_url**：字符串，存储封面图片的相对路径或 URL（来源为用户上传、文件解析或系统生成）。
- **category\_id**：外键，关联分类表。
- **file\_path / file\_format / file\_size**：存储物理文件路径、格式后缀及字节大小。
- **file\_hash**：字符串，存储文件的 SHA-256 哈希值，用于溯源和防篡改。
- **uploader\_id / upload\_ip**：记录上传者 ID 及脱敏后的上传 IP。
- **status**：整数，标识审核状态（待审核/已通过/已驳回）。

#### 4.2.3 评论与互动表 (comment, comment\_like, favorite)

- **comment 表**：包含 id、book\_id（外键）、user\_id（外键）、content（评论内容）、parent\_id（父评论ID，用于实现嵌套回复）、like\_count（冗余的点赞数）。
- **comment\_like 表**：包含 comment\_id 和 user\_id，并建立**联合唯一索引**，从数据库层面防止重复点赞。
- **favorite 表**：包含 book\_id 和 user\_id，同样建立**联合唯一索引**，防止重复收藏。

#### 4.2.4 溯源与版本表 (trace\_log, version\_history)

- **trace\_log 表**：记录书籍生命周期内的所有关键动作（如上传、审核、下载）。包含 book\_id、action（动作类型）、operator\_id（操作人）、detail（JSON 格式的详细信息）。
- **version\_history 表**：当同一本书籍被覆盖更新时，记录历史版本。包含 book\_id、version\_no（版本号）、file\_hash（该版本对应的哈希值）、change\_log（变更说明）。

#### 4.2.5 监控与日志表 (system\_log, frontend\_monitor)

- **system\_log 表**：记录后端接口请求日志、异常堆栈、响应耗时。
- **frontend\_monitor 表**：记录前端上报的页面加载性能指标、JS 错误信息及用户浏览器环境。

---

## 五、 核心业务流程设计

### 5.1 登录与自动注册流程

1. 客户端提交用户名与密码。
2. 后端接收请求，查询用户名是否已存在。
3. **若不存在**：后端使用 BCrypt 对密码进行加密，将新用户信息写入 `sys_user` 表，随后进入签发 Token 流程。
4. **若存在**：后端比对密码哈希值。若匹配，进入签发 Token 流程；若不匹配，返回认证失败提示。
5. 签发 JWT Token，将 Token 及用户基础信息（不含密码）返回给客户端。

### 5.2 电子书上传与封面处理流程

1. 客户端提交电子书文件及可选的封面图片。
2. 后端校验文件格式、大小是否符合系统配置限制。
3. **封面处理分支**：
  - **分支 A（用户上传了封面）**：直接将封面图片存储至文件服务器，获取封面 URL。
  - **分支 B（用户未上传封面）**：后端调用解析组件读取电子书文件内部元数据。若提取到内置封面，则存储并获取 URL；若未提取到，后端调用绘图组件，使用书名和作者文字生成一张默认背景图，存储并获取 URL。
4. 后端计算电子书文件的 SHA-256 哈希值。
5. 校验该哈希值是否已存在。若存在，提示用户选择“作为新版本上传”或“放弃上传”。
6. 将电子书文件持久化至本地磁盘。
7. 将书籍元数据、封面 URL、文件哈希等信息写入 `book` 表，同时向 `trace_log` 和 `version_history` 表写入初始记录。
8. 返回上传结果及书籍溯源编号。

### 5.3 书籍溯源与防篡改校验流程

1. 用户在前端触发“溯源校验”请求。
2. 后端根据书籍 ID 查询 `book` 表获取记录的 `file_hash` 及物理 `file_path`。
3. 后端读取物理文件，实时重新计算其 SHA-256 哈希值。
4. 比对实时哈希值与数据库记录：
  - **一致**：返回“文件完整，未被篡改”状态。
  - **不一致**：返回“文件异常，可能已被篡改”告警，并向 `system_log` 写入安全告警日志。
5. 后端聚合 `trace_log` 和 `version_history` 数据，按时间轴排序后返回给前端渲染溯源面板。

---

## 六、 接口设计规范

### 6.1 RESTful 规范约束

- 所有 API 路径以 `/api/v1` 为前缀。
- 使用标准 HTTP 方法：GET（查询）、POST（创建/复杂查询）、PUT（全量更新）、PATCH（部分更新）、DELETE（删除）。
- 统一响应数据结构：包含 `code`（业务状态码）、`message`（提示信息）、`data`（业务数据载荷）。
- 统一异常处理：全局捕获异常，返回标准化的错误响应，避免向客户端暴露后端堆栈信息。

### 6.2 核心 API 清单描述

- **认证模块**：`POST /auth/login`（登录/自动注册）、`POST /auth/refresh`（Token 续签）。
- **书籍模块**：`POST /books/upload`（ multipart/form-data 格式，上传书籍与封面）、`GET /books`（分页查询书籍列表）、`GET /books/{id}/trace`（获取溯源信息）。
- **互动模块**：`POST /comments`（发表评论）、`POST /comments/{id}/like`（点赞/取消点赞）、`POST /favorites`（收藏/取消收藏）。
- **监控模块（管理端专属）**：`GET /monitor/backend/metrics`（获取 JVM 及系统指标）、`GET /monitor/logs/stream`（建立 WebSocket 连接获取实时日志）。

---

## 七、 非功能性设计

### 7.1 安全设计

- **密码安全**：严禁明文存储，强制使用 BCrypt 算法加盐哈希。
- **接口鉴权**：除登录和公开查询接口外，所有接口均需校验 JWT Token。管理端接口增加角色权限校验（RBAC）。
- **防攻击策略**：前端利用 Vue 的模板转义机制防御 XSS 攻击；后端使用 ORM 框架的参数化查询防御 SQL 注入；对上传文件进行严格的 MIME 类型和文件头校验，防止恶意脚本上传。
- **数据脱敏**：日志和溯源信息中记录的 IP 地址需进行掩码脱敏处理。

### 7.2 性能与并发设计

- **SQLite 优化**：强制开启 WAL（Write-Ahead Logging）模式，将读写锁分离，大幅提升并发读取能力。合理配置 HikariCP 连接池参数，避免连接耗尽。
- **文件 IO 优化**：大文件上传采用分片上传机制，前端分片计算哈希，后端合并，避免大文件占用过多内存和导致请求超时。
- **缓存策略**：对分类树、热门书籍列表等读多写少的数据，在内存中（如 Caffeine）进行短时缓存，降低数据库查询压力。

### 7.3 可观测性设计

- **全链路追踪**：为每个 HTTP 请求生成唯一的 TraceID，贯穿前端请求、后端日志及数据库操作，便于问题排查。
- **指标采集**：通过 Micrometer 将系统指标暴露给 Actuator，管理端定时拉取并绘制趋势图表。

---

## 八、 部署与运维方案

### 8.1 部署架构

采用单机 Nginx 反向代理 + Spring Boot 单体应用的轻量级部署模式。

- **Nginx**：负责托管 Vue 打包后的静态资源，并将 `/api` 开头的请求反向代理至后端服务。配置 Gzip 压缩以提升静态资源加载速度。
- **Spring Boot**：以内嵌 Tomcat 运行，通过 `nohup` 或 `systemd` 守护进程管理。

### 8.2 数据备份策略

鉴于 SQLite 为单文件数据库，制定以下备份策略：

- **定时物理备份**：通过 Linux Crontab 定时任务，每日凌晨低峰期将 `.db` 数据库文件及 WAL 文件压缩打包，备份至异地存储或独立的备份磁盘。
- **文件存储备份**：对电子书物理文件目录进行定期增量同步。

### 8.3 容量与扩展规划

- 初期单台 2核4G 云服务器即可满足需求。
- 当 SQLite 并发写入出现明显瓶颈，或单文件体积超过 10GB 时，启动数据库迁移预案，将 SQLite 平滑迁移至 MySQL 或 PostgreSQL。
- 当本地磁盘空间不足时，将文件存储模块切换至阿里云 OSS 或 MinIO 分布式对象存储。

